import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:mobx_slidy_example/app/modules/mobx/mobx_page.dart';

main() {
  testWidgets('MobxPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(MobxPage(title: 'Mobx')));
    //  final titleFinder = find.text('Mobx');
    //  expect(titleFinder, findsOneWidget);
  });
}
