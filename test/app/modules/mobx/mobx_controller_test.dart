import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:mobx_slidy_example/app/modules/mobx/mobx_module.dart';

void main() {
  initModule(MobxModule());
  // MobxController mobx;
  //
  setUp(() {
    //     mobx = MobxModule.to.get<MobxController>();
  });

  group('MobxController Test', () {
    //   test("First Test", () {
    //     expect(mobx, isInstanceOf<MobxController>());
    //   });

    //   test("Set Value", () {
    //     expect(mobx.value, equals(0));
    //     mobx.increment();
    //     expect(mobx.value, equals(1));
    //   });
  });
}
